name: account_domain
description: Local account, guest merge, and deletion cooling-off domain model.
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
